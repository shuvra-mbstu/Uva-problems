#include<bits/stdc++.h>
using namespace std;
#include<string.h>
int main ()
{
    char input[10000000000];
    int i, j, k, len =0;
    gets(input);
     {
     len = strlen(input)-1;
     for(i=0; i<len; i++)
     {
        printf("%s",input[i]-7);
     }
     }

}
