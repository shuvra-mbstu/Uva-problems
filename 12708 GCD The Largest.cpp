#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long int num, test, res;
    scanf("%lld",&test);
    while(test--)
    {
        scanf("%lld",&num);
        printf("%lld\n",num/2);
    }
}
