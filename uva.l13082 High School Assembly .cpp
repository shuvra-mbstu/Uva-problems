#include<bits/stdc++.h>
using namespace std;
int main()
{
    int test,arr[10007],sorta[10007],num,i,j,k,uniq;
    scanf("%d",&test);
    for(i=0;i<test;i++)
    {
        memset(arr,0,sizeof(arr));
        j=0;
        scanf("%d",&num);
        while(num--)
        {
            j++;
            scanf("%d",&arr[j]);
            //sorta[]
        }
    }
}
